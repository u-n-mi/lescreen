<section>
<div class="container">
  <div class="row">
    <div class="col-md-6">
      <hr>
      <h2>Have a<br/> nice day!</h2>
      <br>
      <%= image_tag('logo.png', :size => "125x150") %>

      <br>
      
    </div>
    <div class="col-md-6">
      <hr>
      <h2 class="text-justify">Account login</h2>
      <span>Access your account.</span>
      <hr>
      <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
      <div class="form-inputs">
        <%= f.input :email,
                required: false,
                autofocus: true,
                input_html: { autocomplete: "email" } %>
        <%= f.input :password,
                required: false,
                input_html: { autocomplete: "current-password" } %>
        <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      </div>
      <div class="form-actions"><br>
        <button class="btn btn-primary sign-up-btn"><%= f.button :submit, "Log in" %></button>
      </div>
      <div class="devise-links">
      <br><%= render "devise/shared/links" %>
      </div>
      <% end %>
    </div>
  </div>
</div>
</section>

